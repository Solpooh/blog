name: Deploy Front and Back

on:
  push:
    branches:
      - main

jobs:
  deploy-front:
    name: Deploy Frontend to Web EC2
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Write private key to file
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key

      - name: Check deploy_key format
        run: |
          cat ~/.ssh/deploy_key
          file ~/.ssh/deploy_key

      - name: Test private key with openssl
        run: openssl rsa -in ~/.ssh/deploy_key -check
  
      - name: Start SSH agent and add key
        run: |
          eval "$(ssh-agent -s)"
          echo "${{ secrets.DEPLOY_KEY }}" | ssh-add -

      - name: Deploy to Web EC2
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@15.164.138.8 << 'EOF'
            cd ~/blog/board-front
            git pull origin main
            pm2 restart static-page-server-3000
          EOF

  deploy-back:
    name: Deploy Backend to WAS EC2
    runs-on: ubuntu-latest
    needs: deploy-front
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Write private key to file
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          
      - name: Start SSH agent and add key
        run: |
          eval "$(ssh-agent -s)"
          echo "${{ secrets.DEPLOY_KEY }}" | ssh-add -

      - name: Deploy to WAS EC2
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@43.203.33.213 << 'EOF'
            cd ~/blog/board-back
            git pull origin main
            ./gradlew clean build
            pm2 restart board-back
          EOF
